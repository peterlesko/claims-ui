// import {render, screen} from "@testing-librery/react"
// import userEvent from "@testing-library/user-event";
// import App from "../App";

// test ("check navigation to the assess c;laims page is working", async () => {
//   render(<App />);
//   const assessClaimsLink = screen.queryByText("ASSESS CLAIMS");
//   userEvent.click(assessClaimsLink);
//   // const heading = await screen.findByRole("heading", {}, 2000);
//   const heading = await screen.findByTestId("h2", {}, 2000);
//   expect(heading).toHaveTextContext("Assess claims");


// })  